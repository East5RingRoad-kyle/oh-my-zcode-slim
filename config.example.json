{
  "$comment": [
    "oh-my-zcode-slim model configuration (optional).",
    "Copy to ~/.agents/oh-my-zcode-slim.json and edit as needed.",
    "If this file is absent, every role inherits the session model (recommended).",
    "ZCode subagents may not support per-agent model selection;",
    "'model' entries are advisory metadata for now and are ignored gracefully."
  ],
  "preset": "default",
  "presets": {
    "default": {
      "explorer":  { "model": null, "note": "null = inherit session model" },
      "oracle":    { "model": null },
      "librarian": { "model": null },
      "fixer":     { "model": null },
      "designer":  { "model": null }
    },
    "custom": {
      "$comment": "Example: pin specific models per role. Fill in your own.",
      "explorer":  { "model": "zhipu/glm-4.7-flash" },
      "oracle":    { "model": "zhipu/glm-4.7" },
      "librarian": { "model": "zhipu/glm-4.7-flash" },
      "fixer":     { "model": "zhipu/glm-4.7" },
      "designer":  { "model": "zhipu/glm-4.7" }
    }
  }
}
